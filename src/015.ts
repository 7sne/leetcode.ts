// iterate over array
//

function threeSum(nums: number[]): number[][] {
    nums = nums.sort((a, b) => a - b);
    console.log(nums);
    for (const num of nums) {
        for (const num of nums) {
        }
    }
    return [];
}

export const main015 = (async function main014() {
    const nums = [-1, 0, 1, 2, -1, -4];
    // console.log(threeSum(nums))
})().catch(e => console.error(e));
